import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobey.starter',
  appName: 'mobey',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
